### Manus 팀의  KV캐시 실천 5원칙
#### KV-cache 최적화와 접두어 안정성이 최우선.
  - 시스템 프롬프트와 도구 정의는 가능한 한 고정.
#### 도구 목록을 중간에 바꾸지 말 것. 
  - 에이전트가 실행 중에 도구를 동적으로 추가하거나 제거하면, 도구 정의가 접두어에 포함되어 있으므로 캐시가 깨집니다. 
  - 대신 사용 가능한 도구를 상태에 따라 활성/비활성으로 전환하는 방식을 씁니다.
#### 통제된 다양성. 
  - 에이전트가 같은 입력에 같은 행동만 반복하면 루프에 빠질 수 있습니다. 
  - 변주를 통해, 모델이 새로운 시도를 하도록 유도합니다. 
  - **다만 접두어 안정성을 해치지 않는 범위 내에서,** 응답 포맷이나 표현을 약간씩 변주합시다. 
    - (예: JSON 키 순서를 바꾸거나, 같은 의미를 다른 문장으로 표현) 
#### 프로덕션 우선 접근. 
  - 모델을 처음부터 다시 훈련(fine-tuning)하는 것보다, 
  - 컨텍스트 엔지니어링으로 개선하는 쪽이 반복 속도가 훨씬 빠릅니다. 
  - 개선 사항을 몇 시간 안에 배포할 수 있습니다.
#### 모델이 바뀌어도 하네스는 유지. 
  - 컨텍스트 관리 시스템을 특정 모델에 종속되지 않게 설계합니다. 
  - Claude에서 GPT로, GPT에서 Gemini로 모델을 교체해도 하네스는 그대로 작동해야 합니다.
